# Vendor assets

Static browser dependencies used by the GitHub Pages build.

- `d3` 7.9.0, ISC license
- `onnxruntime-web` 1.20.1, MIT license

These files are copied from the npm packages so the Pages app does not depend on a runtime CDN.
